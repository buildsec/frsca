global:
  spire:
    clusterName: frsca
    trustDomain: example.org

spiffe-oidc-discovery-provider:
  tls:
    spire:
      enabled: false
    externalSecret:
      enabled: true
      secretName: oidc-cert
